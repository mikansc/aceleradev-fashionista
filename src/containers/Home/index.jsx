import React from 'react';

import { Product, Loading } from '../../components/';

const Home = () => {
  return (
    <div className="container">
      <Product />
    </div>
  );
};

export default Home;
